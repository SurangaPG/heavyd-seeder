default:
  default:
    default:
      "host": "localhost"
      "port": 3306
      "username": "root"
      "password": "root"
      "database": ${project.group}_${project.version}_${project.machineName}
      "driver": "mysql"
      "namespace": Drupal\Core\Database\Driver\mysql